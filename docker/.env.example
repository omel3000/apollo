# Apollo - Przykładowa konfiguracja środowiska
# Skopiuj ten plik jako .env i uzupełnij własnymi wartościami

# === DATABASE CONFIGURATION ===
POSTGRES_USER=apollo
POSTGRES_PASSWORD=ZMIEN_TO_NA_SILNE_HASLO  # Użyj: openssl rand -base64 24
POSTGRES_DB=apollo_test_db  # apollo_prod_db dla produkcji

# === DATABASE URL ===
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://apollo:ZMIEN_TO_NA_SILNE_HASLO@postgres-db:5432/apollo_test_db

# === JWT / AUTHENTICATION ===
SECRET_KEY=ZMIEN_TO_NA_LOSOWY_KLUCZ  # Użyj: openssl rand -hex 32
ACCESS_TOKEN_EXPIRE_MINUTES=60

# === APPLICATION ENVIRONMENT ===
ENVIRONMENT=development  # development | production
LOG_LEVEL=debug  # debug | info | warning | error

# === CORS POLICY ===
# Dozwolone domeny (oddzielone przecinkami)
ALLOWED_ORIGINS=http://localhost,http://localhost:80

# Produkcja:
# ALLOWED_ORIGINS=https://apollo.twoja-domena.pl

# === SWAGGER UI / API DOCUMENTATION ===
ENABLE_DOCS=true  # true = /docs i /redoc dostępne | false = wyłączone
# Na produkcji możesz zostawić true jeśli chcesz mieć dostęp do dokumentacji API
